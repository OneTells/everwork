[project]
name = "everwork"
version = "2.3.2"

requires-python = ">=3.12,<4.0"

dependencies = [
    "hiredis~=3.2.1",
    "loguru~=0.7.3",
    "orjson~=3.10.18",
    "pydantic~=2.11.7",
    "redis~=6.2.0"
]

[dependency-groups]
fast = [
    "uvloop~=0.21.0 ; sys_platform == 'linux'"
]
