[project]
name = "everwork"
version = "2.1.4"

requires-python = ">=3.12,<4.0"

dependencies = [
    "hiredis~=3.1.0",
    "loguru~=0.7.3",
    "orjson~=3.10.16",
    "pydantic~=2.11.1",
    "redis~=5.2.1",
    "uvloop~=0.21.0"
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
