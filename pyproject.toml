[project]
name = "everwork"
version = "2.3.3"

requires-python = ">=3.12,<4.0"

dependencies = [
    "hiredis~=3.2.1",
    "loguru~=0.7.3",
    "orjson~=3.11.3",
    "pydantic~=2.11.9",
    "redis~=6.4.0",
]

[dependency-groups]
fast = [
    "uvloop~=0.21.0 ; sys_platform == 'linux'"
]
